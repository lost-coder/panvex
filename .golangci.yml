version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gocritic
    - gosec
    - misspell
    - unconvert
  settings:
    gosec:
      excludes:
        - G104
        - G112
        - G115
        - G505
  exclusions:
    rules:
      - linters:
          - errcheck
        text: "Error return value of.*Close.*is not checked"
      - linters:
          - errcheck
        text: "tx.Rollback"
      - linters:
          - errcheck
        text: "conn.CloseNow"
      - linters:
          - unused
        text: "func.*is unused"
      - linters:
          - gosimple
        text: "S1016"
      - linters:
          - gocritic
        text: "ifElseChain"
      - linters:
          - govet
        text: "lostcancel"
