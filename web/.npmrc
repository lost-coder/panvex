# eslint-plugin-jsx-a11y@6.10.2 declares a peer range of eslint up to ^9, but
# this project tracks eslint@10. Until upstream relaxes the peer range, allow
# `npm ci` (and `npm install`) to proceed despite the peer mismatch. Removing
# this flag will break CI's strict `npm ci` step.
legacy-peer-deps=true
