// Features are intentionally NOT aggregated into a single barrel — each
// slice owns its own public surface and must be imported by explicit
// path so cross-feature coupling stays visible in diffs.
export {};
